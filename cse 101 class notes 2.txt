Make a program to enter your full name, registration no, roll no,age, last semester marks. blood group.

#include<stdio.h>
int main()
{
	char name[30],roll[20],blood[3];
	int age;
	long int regno;
	float marks;
	printf("Enter your name:-");
	gets(name);
	puts(name); 
      printf("Enter your roll no:-");
	gets(roll);
	puts(roll); 
	printf("Enter your blood group:-");
	gets(blood);
	puts(blood); 
	printf("Enter your age:-");
	scanf("%d",&age);
	printf("%d\n",age);
	printf("Enter your registration no:-");
	scanf("%ld",&regno);
	printf("%ld\n",regno);
	printf("Enter your marks:-");
	scanf("%2f",&marks);
	printf("%2f",marks);
	
	
	return 0;
}


Make a list that enters number of students in 10 sections
#include<stdio.h>
int main()
{
	int section[10];
	printf("Enter no of students in each section\n");
	for(int i;i<10;i++)
	{
		scanf("%d",&section[i]);
	}
	for(int i;i<10;i++)
	{
	    printf("section[%d] -%d\n",i,section[i]);
	}
	return 0;
}

Make a list that enters cost of 6 books in your bookshelf.


And at last calculate total price 

